# TODO

let's think of how to implement the compiler using ralph loop ( see
`/Users/adelnizamutdinov/github/ralph`)

the compiler is gonna be a single pass with no AST.

help me come up with intermediate stop conditions/ralph feedbacks.

old compiler (see
`/Users/adelnizamutdinov/responsible/responsible/packages/generator`) is just
1900 lines of typescript (with tests), so relatively simple stuff.
