---
name: jsonc
description: Never use this for `.json`; apply it only to `.jsonc`
---

# JSONC

- Never use `//` comments.
- Never use `/*` for multiline comments. Use `/**` instead, with each line starting with `*`.
